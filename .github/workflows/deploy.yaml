name: mailio-ai-search

on:
  push:
    branches: [ test, production ]

permissions:
  contents: read

jobs:
  deploy-web-app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: UPDATE SHA
        run: echo $GITHUB_SHA > $GITHUB_WORKSPACE/_meta

      - name: Set env to test
        if: endsWith(github.ref, '/test')
        run: |
          echo "ENVIRONMENT=test" >> $GITHUB_ENV

      - name: Set env to production
        if: endsWith(github.ref, '/production')
        run: |
          echo "ENVIRONMENT=production" >> $GITHUB_ENV

      - name: Build Docker image
        run: |
          docker build \
            . --file Dockerfile --tag registry.digitalocean.com/mailio/mailio-ai:${ENVIRONMENT}-$(echo $GITHUB_SHA | head -c7)

      - name: Install DigitalOcean doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry
        run: doctl registry login --expiry-seconds 600

      - name: Push image to DigitalOcean Container Registry
        run: docker push registry.digitalocean.com/mailio/mailio-ai:${ENVIRONMENT}-$(echo $GITHUB_SHA | head -c7)

      - name: Update deployment file
        run: TAG=${ENVIRONMENT}-$(echo $GITHUB_SHA | head -c7) && sed -i 's|<IMAGE>|registry.digitalocean.com/mailio/mailio-ai:'${TAG}'|' $GITHUB_WORKSPACE/deployment/private/03-deployment.yaml

      - name: Save DigitalOcean kubeconfig (production)
        if: endsWith(github.ref, '/production')
        run: |
          doctl kubernetes cluster kubeconfig save mail-io

      - name: Save DigitalOcean kubeconfig (test)
        if: endsWith(github.ref, '/test')
        run: |
          doctl kubernetes cluster kubeconfig save mailio-io

      - name: Store DO authentication token as a secret in cluster
        run: doctl registry kubernetes-manifest | kubectl apply -f -

      - name: Update configmap
        if: endsWith(github.ref, '/test')
        run: kubectl apply -f $GITHUB_WORKSPACE/deployment/private/01-configmap.yaml
    
      - name: Update configmap
        if: endsWith(github.ref, '/production')
        run: kubectl apply -f $GITHUB_WORKSPACE/deployment/private/production/01-configmap.yaml


      - name: Update secrets
        if: endsWith(github.ref, '/test')
        run: kubectl apply -f $GITHUB_WORKSPACE/deployment/private/02-secrets.yaml

      - name: Update secrets
        if: endsWith(github.ref, '/production')
        run: kubectl apply -f $GITHUB_WORKSPACE/deployment/private/production/02-secrets.yaml

      - name: Deploy to DigitalOcean Kubernetes
        run: kubectl apply -f $GITHUB_WORKSPACE/deployment/private/03-deployment.yaml

      - name: Update pod disruption budget
        run: kubectl apply -f $GITHUB_WORKSPACE/deployment/private/04-poddisruptionbudget.yaml

      - name: Verify deployment
        run: kubectl get ds